/**
 * Casos de uso (camada de aplicação) — a seam primária de testes.
 *
 * Cada caso de uso recebe um contexto transacional e o ator
 * (`actorUserId` + `actingAs`) e retorna um `Result` tipado (ADR-0001).
 * Nenhuma regra de negócio vive em componentes ou rotas.
 *
 * Os casos de uso concretos entram a partir do ticket 06.
 */

export {};
